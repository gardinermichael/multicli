Read ./TESTS.md and execute the full verification suite by calling the MCP tools provided. Do not attempt to read source code or navigate to parent directories; the tests are behavioral and must be performed by invoking the backend tools (e.g., {cli-name}-ask, {cli-name}-list-models).


Follow this exact workflow:

1. Execute Suites: Run all tests in the order specified in the Execution Order section (Phase 1, then Phase 2, then
      Phase 3).
2. Record Results: Document the outcome (PASS/FAIL/SKIP) for every single test case listed in the document.

3. Summary: Provide the final completed Summary Checklist table at the end of the session.

## CRITICAL NOTES:
- Some of the tests are designed to fail. Run each test as instructed, even if you are certain it will fail.
- Run each test one at a time, in the order specified. Do not skip tests.
- Do not explore any files outside of the directory containing the TESTS.md file
